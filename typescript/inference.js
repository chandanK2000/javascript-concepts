"use strict";
var data = 10; // this will automatically take number type because of inference  if want to declare second times string  then it will throw the error message.
var str = "hello";
var abc; // it will take any type automatically
var ram = true;
ram = 10; // this will throw the error because of already this variable took boolean value;
ram = "hello"; // this will throw the error because of already this variable took boolean value;
// let num:5=10; // because it took type is 5 that is reserved
var num = 5; //it will take 5
var val = 20;
var values = 10;
values = 34; // it is const that'why it is throwing the error
val = 30; // it will change because it is let we can change in the javascript also;
